import { describe, expect, it } from 'vitest';
import { calculateScore, duplicatePairKey, matchesScoreRule, normalizeEmail, normalizePhone } from '../../src/modules/crm/domain/crm.types.js';
describe('CRM domain',()=>{it('normalizes contact identifiers',()=>{expect(normalizeEmail(' A@Example.COM ')).toBe('a@example.com');expect(normalizePhone('+62 (812) 00-11')).toBe('+628120011');});it('creates deterministic duplicate key',()=>{expect(duplicatePairKey('b','a')).toBe('a:b');});it('evaluates score rules deterministically',()=>{const rules=[{code:'qualified',field:'status',operator:'EQ',value:'QUALIFIED',points:25}];expect(matchesScoreRule({values:{status:'QUALIFIED'}},rules[0])).toBe(true);expect(calculateScore({values:{status:'QUALIFIED'}},rules)).toEqual({score:25,factors:[{code:'qualified',points:25,explanation:'status EQ QUALIFIED'}]});});});
