name: Apply Exact CMS Prettier Output

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  apply:
    if: github.event.before == '015ed375d89a3b4b4da5189cdaa5c7f45800b950'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout main
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - name: Download exact Prettier output
        uses: actions/download-artifact@v4
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          repository: Fakhrialfi9/estate-pro
          run-id: 33429627345
          artifact-ids: 9772105275
          path: /tmp/prettier-cms
      - name: Apply formatted CMS files
        run: |
          test -f /tmp/prettier-cms/prettier-cms-output/content.service.ts
          test -f /tmp/prettier-cms/prettier-cms-output/prisma-article.repository.ts
          test -f /tmp/prettier-cms/prettier-cms-output/prisma-content-resource.repository.ts
          cp /tmp/prettier-cms/prettier-cms-output/content.service.ts src/modules/content/application/content.service.ts
          cp /tmp/prettier-cms/prettier-cms-output/prisma-article.repository.ts src/modules/content/infrastructure/persistence/prisma-article.repository.ts
          cp /tmp/prettier-cms/prettier-cms-output/prisma-content-resource.repository.ts src/modules/content/infrastructure/persistence/prisma-content-resource.repository.ts
          git add src/modules/content/application/content.service.ts src/modules/content/infrastructure/persistence/prisma-article.repository.ts src/modules/content/infrastructure/persistence/prisma-content-resource.repository.ts
          git diff --cached --check
          if git diff --cached --quiet; then exit 0; fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git commit -m 'style(cms): apply exact Prettier formatting'
          git push origin HEAD:main
