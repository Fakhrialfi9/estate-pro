name: Format Content Source

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  format:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm install --global npm@11.18.0
      - run: npm ci
      - run: npx prettier --write 'src/modules/content/**/*.ts'
      - run: npx prettier --check 'src/modules/content/**/*.ts'
      - name: Commit formatted content source
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add src/modules/content
          if git diff --cached --quiet; then exit 0; fi
          git diff --cached --check
          git commit -m 'style(cms): format content module source'
          git push origin HEAD:main
